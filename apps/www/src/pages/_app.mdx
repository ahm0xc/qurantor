import PagesLayout from "~/components/pages-layout";

import "~/styles/docs.css";
import "~/styles/docs-variables.css";

export default function App({ Component, pageProps }) {
  return (
    <PagesLayout>
      <Component {...pageProps} />
    </PagesLayout>
  );
}
