import { Alert, Button } from "@lemonsqueezy/wedges";
import ThemeToggle from "~/components/theme-toggle";

export default function Home() {
  return (
    <main>
      <Button>You're awesome!</Button>
      <ThemeToggle />
    </main>
  );
}
